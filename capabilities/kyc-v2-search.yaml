naftiko: 1.0.0-alpha2
info:
  label: KYC V2 API — Search
  description: 'KYC V2 API — Search. 1 operations. Lead operation: Search for individual profiles. Self-contained Naftiko
    capability covering one Frankieone business surface.'
  tags:
  - Frankieone
  - Search
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FRANKIEONE_API_KEY: FRANKIEONE_API_KEY
capability:
  consumes:
  - type: http
    namespace: kyc-v2-search
    baseUri: https://api.uat.frankie.one
    description: KYC V2 API — Search business capability. Self-contained, no shared references.
    resources:
    - name: v2-search-serviceprofiles
      path: /v2/search/serviceprofiles
      operations:
      - name: searchindividuals
        method: POST
        description: Search for individual profiles
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.FRANKIEONE_API_KEY}}'
  exposes:
  - type: rest
    namespace: kyc-v2-search-rest
    port: 8080
    description: REST adapter for KYC V2 API — Search. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/v2/search/serviceprofiles
      name: v2-search-serviceprofiles
      description: REST surface for v2-search-serviceprofiles.
      operations:
      - method: POST
        name: searchindividuals
        description: Search for individual profiles
        call: kyc-v2-search.searchindividuals
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: kyc-v2-search-mcp
    port: 9090
    transport: http
    description: MCP adapter for KYC V2 API — Search. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: search-individual-profiles
      description: Search for individual profiles
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: kyc-v2-search.searchindividuals
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
